
#include <iostream>

int main()
{
    // Quote: ("), backslash: (\), newline: (\n)
    std::cout << "Quote: (\"), backslash: (\\), newline: (\\n)";
    // std::cout << R"(Quote: ("), backslash: (\), newline: (\n))";
    return 0;
}
